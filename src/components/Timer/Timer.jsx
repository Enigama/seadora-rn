import React from "react";
import { View, StyleSheet } from "react-native";
import { CustomText } from "../custom-text/CustomText";

export const Timer = () => {
  return (
    <View>
      <CustomText text={"timer"} />
    </View>
  );
};
